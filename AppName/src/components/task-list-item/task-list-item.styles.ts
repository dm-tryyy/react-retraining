import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderRadius: 8,
    paddingEnd: 6,
    paddingTop: 6,
    paddingBottom: 6,
    flexDirection: 'row',
    backgroundColor: '#FFFFFF',
    marginTop: 6,
  },
  image: {
    width: 16,
    height: 16,
  },
});
